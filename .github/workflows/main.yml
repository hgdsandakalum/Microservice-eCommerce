name: GitHub Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: pwd
      - name: List files in the repository
        working-directory: /home/runner/work/Microservice-eCommerce/Microservice-eCommerce/test/
        run: |
          ls ${{ github.workspace }}
